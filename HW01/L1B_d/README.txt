/*
README.txt
  	Pi is being computed is using the preprocessor directives iostream iomanip and cmath, pi is
calculated by the formulla 4 time the arctangent of 1. The precision is set to the 10th digit using the 
 setprecision manipulator.
        The code is compilled using the g++ function then the file name in the command window. After the	the program was compiled then one can use ./a.out then the file name to run it.
*/
